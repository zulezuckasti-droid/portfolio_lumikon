import { ContactSection } from "@/components/sections/contact-section";

/**
 * Kontakt / CTA sekcija početne strane (PRD §6.7, §7, §8).
 */
export function ContactCta() {
  return <ContactSection id="kontakt" showBorderTop headingLevel="h2" />;
}
